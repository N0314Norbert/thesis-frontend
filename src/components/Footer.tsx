import { AppBar, CssBaseline, Toolbar } from '@mui/material';

function Footer() {
	return (
		<CssBaseline>
			<AppBar color="primary" position="relative" className="footer">
				<Toolbar></Toolbar>
			</AppBar>
		</CssBaseline>
	);
}
export default Footer;
